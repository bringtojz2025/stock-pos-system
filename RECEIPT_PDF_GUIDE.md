# 📋 ระบบสร้างและปริ้นใบเสร็จ PDF

## คำอธิบายระบบ

ระบบนี้ออกแบบมาเพื่อให้สามารถสร้าง ปริ้น และจัดเก็บใบเสร็จ PDF อัตโนมัติเมื่อลูกค้าชำระเงิน

## 🎯 คุณสมบัติหลัก

### 1. **สร้างใบเสร็จ PDF อัตโนมัติ**
- เมื่อกดปุ่ม "💰 ชำระเงิน / ตัดสต็อก" โปรแกรมจะสร้างไฟล์ PDF ใบเสร็จ
- ใบเสร็จจะบันทึกลงในโฟลเดอร์ `receipts/` พร้อมชื่อเลขที่ใบเสร็จ
- เช่น: `JZ20260109000001.pdf`

### 2. **รูปแบบใบเสร็จ**
ใบเสร็จมี 4 โซน (ออกแบบสำหรับเครื่องปริ้น 80mm):

```
╔══════════════════════════╗
║   ร้านอะไหล่รถ JZ        ║  ← ชื่อร้าน
║   JZ Auto Parts          ║
╠══════════════════════════╣
║ เลขที่: JZ20260109000001 ║  ← เลขที่ใบเสร็จ
║ วันที่: 09/01/2026       ║
║ เวลา: 15:30:45           ║
╠══════════════════════════╣
║ สินค้า    | จำ | ราคา   ║  ← รายการสินค้า
║ เบรค      | 1  | 250.00║
║ หม้อน้ำ   | 1  | 180.00║
╠══════════════════════════╣
║ ยอดรวม: 430.00 บาท     ║  ← สรุปยอด
║ ส่วนลด: -43.00 บาท     ║
║ ยอดสุดท้าย: 387.00 บาท ║
╠══════════════════════════╣
║ วิธีจ่าย: เงินสด         ║  ← วิธีชำระเงิน
║ โค้ตที่ใช้: DISC10       ║
║ โค้ตใหม่: DISC10-3045   ║
╠══════════════════════════╣
║    [      BARCODE      ] ║  ← บาร์โค้ด
║    JZ20260109000001     ║
╠══════════════════════════╣
║ ขอบคุณสำหรับการเลือก    ║  ← ข้อความปิด
║ THANK YOU FOR YOUR VISIT║
║ www.yourwebsite.com     ║
║ Tel: 0xx-xxx-xxxx       ║
╚══════════════════════════╝
```

### 3. **ระบบปริ้น 2 โหมด**

#### โหมดที่ 1: ปริ้นอัตโนมัติ (ค่าเริ่มต้น: ปิด)
- เมื่อเปิดใช้งาน จะปริ้นใบเสร็จโดยอัตโนมัติหลังจากชำระเงินเสร็จ
- ไม่มีการยืนยันใดๆ ปริ้นเลยทันที

#### โหมดที่ 2: พรีวิวก่อนปริ้น (ค่าเริ่มต้น: เปิด)
- เมื่อชำระเงินเสร็จ จะแสดงข้อความถาม
- ผู้ใช้สามารถเลือกว่าจะปริ้นหรือไม่
- ไฟล์ PDF จะถูกบันทึกไว้เสมอ

### 4. **เครื่องปริ้น 80mm**
- รองรับเครื่องปริ้นความกว้าง 80 มม. (ความยาวหลายไว้ไม่จำกัด)
- สามารถเลือกเครื่องปริ้นที่ติดตั้งบนเครื่องได้
- การตั้งค่าจะบันทึกลงในไฟล์ `printer_config.json`

## 📁 โครงสร้างไฟล์

```
stock-pos-system/
├── app_stock.py                    # โปรแกรมหลัก
├── requirements.txt                # ไลบรารี่ที่ใช้
├── receipts/                       # โฟลเดอร์เก็บใบเสร็จ PDF
│   ├── JZ20260109000001.pdf       # ใบเสร็จ
│   ├── JZ20260109000001_barcode.png # บาร์โค้ด
│   └── ...
├── receipt_settings.json           # ตั้งค่าปริ้นใบเสร็จ
└── printer_config.json             # ตั้งค่าเครื่องปริ้น
```

## 🔧 ติดตั้งและใช้งาน

### ขั้นตอนที่ 1: ติดตั้งไลบรารี่
โปรแกรมได้เพิ่มไลบรารี่ที่จำเป็นแล้ว:
- `reportlab` - สร้าง PDF
- `python-barcode` - สร้างบาร์โค้ด

ติดตั้งด้วยคำสั่ง:
```bash
pip install -r requirements.txt
```

### ขั้นตอนที่ 2: ตั้งค่าเครื่องปริ้น
1. ไปที่เมนู **📊 Dashboard** (แท็บที่ 4)
2. มองหาปุ่ม "🖨️ ตั้งค่าเครื่องปริ้น"
3. เลือกเครื่องปริ้น 80mm จากรายการ
4. กด "บันทึก" - จะบันทึกลง `printer_config.json`

### ขั้นตอนที่ 3: เลือกโหมดปริ้น
1. ก่อนใช้งาน ให้เลือกโหมดปริ้น:
   - **ปริ้นอัตโนมัติ**: โปรแกรมจะปริ้นทันทีเมื่อชำระเงิน
   - **พรีวิวก่อน**: แสดงข้อความถามก่อนปริ้น (ค่าเริ่มต้น)

2. วิธีเปลี่ยนโหมด:
   - ในไฟล์ `receipt_settings.json`:
   ```json
   {
       "auto_print": false  // false = พรีวิว, true = ปริ้นอัตโนมัติ
   }
   ```

### ขั้นตอนที่ 4: ใช้งาน
1. เพิ่มสินค้าในตะกร้า (แท็บ POS)
2. ป้อนจำนวน และราคา
3. กดปุ่ม "💰 ชำระเงิน / ตัดสต็อก"
4. เลือกวิธีชำระเงิน (เงินสด, QR Code, Credit Card)
5. ใส่โค้ตส่วนลด (ถ้ามี)
6. ให้ระบบปริ้นตามโหมดที่เลือก

## 📊 ข้อมูลที่เก็บในใบเสร็จ

| ข้อมูล | รายละเอียด |
|--------|-----------|
| **เลขที่ใบเสร็จ** | JZ20260109000001 |
| **วันที่เวลา** | 09/01/2026 15:30:45 |
| **รายการสินค้า** | ชื่อ, จำนวน, ราคา, รวม |
| **ยอดรวม** | ก่อนลด |
| **ส่วนลด** | ยอดส่วนลดจากโค้ต |
| **ยอดสุดท้าย** | ยอดรวม - ส่วนลด |
| **วิธีชำระเงิน** | เงินสด, QR Code, Credit Card |
| **โค้ตที่ใช้** | โค้ตส่วนลด |
| **โค้ตใหม่** | โค้ตรางวัลใหม่ (ซื้อครบ 200 บาท) |
| **บาร์โค้ด** | Code128 |

## 🎨 ปรับแต่ง

### เปลี่ยนชื่อร้าน
ไปที่ฟังก์ชัน `generate_receipt_pdf()` ในไฟล์ `app_stock.py` บรรทัด ~4275:
```python
# ชื่อร้าน
story.append(Paragraph("ร้านอะไหล่รถ JZ", title_style))
story.append(Paragraph("JZ Auto Parts", normal_style))
```

### เปลี่ยนเบอร์โทรศัพท์และเว็บไซต์
บรรทัด ~4340:
```python
story.append(Paragraph("www.yourwebsite.com", normal_style))
story.append(Paragraph("Tel: 0xx-xxx-xxxx", normal_style))
```

### ปรับขนาดกระดาษ
ส่วน page width (บรรทัด ~4264):
```python
# สำหรับเครื่องปริ้น 80mm
doc = SimpleDocTemplate(pdf_path, pagesize=(80*mm, 300*mm),
```

## 🛠️ Troubleshooting

### ❌ ไม่พบเครื่องปริ้น
- ตรวจสอบว่าเครื่องปริ้นเชื่อมต่อแล้ว
- ไปตั้งค่าเครื่องปริ้นอีกครั้ง
- ลองเลือกเครื่องปริ้นอื่น

### ❌ ไฟล์ PDF ไม่ปริ้น
- ตรวจสอบโหมดปริ้น (อัตโนมัติหรือพรีวิว)
- ดูไฟล์ `receipt_settings.json`
- ลองปริ้นด้วยตัวอักษร

### ❌ บาร์โค้ดไม่แสดง
- ตรวจสอบว่าเลขที่ใบเสร็จถูกต้อง
- ลองสร้างใบเสร็จใหม่

### ❌ ข้อความไทยแสดงไม่ถูก
- ตรวจสอบไฟล์อักษร (Font)
- ฟังก์ชัน `setup_kanit_font()` ควรทำการดาวน์โหลด Kanit font อัตโนมัติ

## 📝 ตัวอย่างไฟล์ JSON

### receipt_settings.json
```json
{
    "auto_print": false
}
```

### printer_config.json
```json
{
    "selected_printer": "Microsoft Print to PDF"
}
```

## 🚀 อัปเดตในอนาคต

พิจารณาเพิ่มเติม:
- [ ] บันทึก PDF ลง Google Drive
- [ ] ส่ง PDF ทาง Email ให้ลูกค้า
- [ ] หลายภาษา (English)
- [ ] QR Code ที่เก็บข้อมูลใบเสร็จ
- [ ] ท้ายใบเสร็จแบบกำหนดเอง

## 📞 ติดต่อ

หากมีปัญหาหรือขออธิบายเพิ่มเติม ติดต่อ: **bringtojz@gmail.com**

---

**ระบบนี้ถูกออกแบบให้ใช้งานง่าย และเหมาะสำหรับร้านอะไหล่รถหรือธุรกิจค้าปลีก** ✨
