# 📋 ระบบสร้างและปริ้นใบเสร็จ PDF - สรุปการอัปเดต

## 🎉 อัปเดตใหม่

ระบบนี้เพิ่มความสามารถในการสร้างและปริ้นใบเสร็จ PDF อัตโนมัติจากระบบ POS

### ✨ คุณสมบัติใหม่

1. **สร้างใบเสร็จ PDF อัตโนมัติ** เมื่อชำระเงิน
2. **ปริ้นใบเสร็จ** ผ่านเครื่องปริ้น 80mm thermal
3. **สองโหมดปริ้น:**
   - **ปริ้นอัตโนมัติ**: ปริ้นทันทีเมื่อชำระเงิน
   - **พรีวิวก่อน**: แสดง dialog ให้ผู้ใช้เลือก
4. **เก็บ PDF** ลงในโฟลเดอร์ `receipts/`
5. **บาร์โค้ด Code128** ในใบเสร็จ

### 📦 ไลบรารี่ที่เพิ่ม

```
reportlab>=4.0.0      # สร้าง PDF
python-barcode>=0.15.0 # สร้างบาร์โค้ด
```

ติดตั้งด้วย:
```bash
pip install -r requirements.txt
```

### 🎨 UI Changes

#### แท็บ POS (ฝั่งซ้าย)
เพิ่มหมวด "⚙️ ตั้งค่าใบเสร็จ" ด้วย:
- 🖨️ **สลับปริ้นอัตโนมัติ** - เปิด/ปิด auto print
- 📁 **เปิดโฟลเดอร์ใบเสร็จ** - ดูไฟล์ PDF ที่บันทึก

### 📁 โครงสร้างไฟล์ใหม่

```
stock-pos-system/
├── receipts/                   # ← โฟลเดอร์ใบเสร็จ (ใหม่)
│   ├── JZ20260109000001.pdf
│   ├── JZ20260109000001_barcode.png
│   └── ...
├── receipt_settings.json       # ← ตั้งค่าปริ้น (ใหม่)
├── RECEIPT_PDF_GUIDE.md        # ← คู่มือผู้ใช้ (ใหม่)
├── RECEIPT_TECHNICAL_DOCS.md   # ← เอกสารเทคนิค (ใหม่)
└── app_stock.py                # อัปเดต
```

### 🔄 Flow การทำงาน

```
1. ผู้ใช้เพิ่มสินค้า → เลือกจำนวน → ใส่ราคา
   ↓
2. กดปุ่ม "💰 ชำระเงิน / ตัดสต็อก"
   ↓
3. ระบบ:
   ├─ บันทึกข้อมูลลง Google Sheets
   ├─ สร้างไฟล์ PDF ใบเสร็จ
   ├─ สร้างบาร์โค้ด
   ├─ บันทึกลง receipts/ folder
   └─ ตรวจสอบโหมดปริ้น
   ↓
4. ถ้าปริ้นอัตโนมัติ → ปริ้นทันที
   ถ้าพรีวิว → แสดง dialog ให้เลือก
   ↓
5. ล้างตะกร้าและกลับสู่ขั้นตอนต่อไป
```

### 🛠️ ฟังก์ชันใหม่

| ฟังก์ชัน | ความหมาย |
|---------|---------|
| `load_receipt_settings()` | โหลดการตั้งค่าจาก JSON |
| `save_receipt_settings()` | บันทึกการตั้งค่าเป็น JSON |
| `generate_receipt_pdf()` | สร้าง PDF ใบเสร็จ |
| `_generate_barcode_image()` | สร้างบาร์โค้ด |
| `show_receipt_preview()` | แสดงพรีวิว PDF |
| `print_receipt()` | ปริ้น PDF |
| `process_receipt_after_checkout()` | ประมวลผลใบเสร็จ |
| `_show_receipt_options()` | แสดงตัวเลือก |
| `get_selected_printer()` | ดึงชื่อเครื่องปริ้น |
| `toggle_receipt_auto_print()` | สลับโหมดปริ้น |

### ⚙️ ตั้งค่าเริ่มต้น

**receipt_settings.json** (สร้างอัตโนมัติ):
```json
{
    "auto_print": false
}
```

**printer_config.json** (จากการตั้งค่าเดิม):
```json
{
    "selected_printer": "Microsoft Print to PDF"
}
```

### 📖 คู่มือการใช้งาน

1. **คู่มือผู้ใช้**: [RECEIPT_PDF_GUIDE.md](RECEIPT_PDF_GUIDE.md)
2. **เอกสารเทคนิค**: [RECEIPT_TECHNICAL_DOCS.md](RECEIPT_TECHNICAL_DOCS.md)

### 🎯 วิธีใช้ (ด่วน)

#### เปิดโหมดปริ้นอัตโนมัติ
1. ไปแท็บ "🛒 ขายหน้าร้าน (POS)"
2. ฝั่งซ้าย มองหา "⚙️ ตั้งค่าใบเสร็จ"
3. กดปุ่ม "🖨️ สลับปริ้นอัตโนมัติ"

#### ดูไฟล์ใบเสร็จ
1. กดปุ่ม "📁 เปิดโฟลเดอร์ใบเสร็จ"
2. ดูไฟล์ PDF และ barcode

#### เปลี่ยนเครื่องปริ้น
1. ไปแท็บ "📊 Dashboard"
2. มองหา "🖨️ ตั้งค่าเครื่องปริ้น"
3. เลือกเครื่องปริ้นจากรายการ
4. กดบันทึก

### 🧪 ทดสอบระบบ

#### Test 1: สร้าง PDF
```python
# ขั้นตอน:
1. เพิ่มสินค้า 1-2 รายการ
2. กดปุ่ม "💰 ชำระเงิน"
3. ตรวจสอบไฟล์ PDF ในโฟลเดอร์ receipts/
```

#### Test 2: ปริ้นอัตโนมัติ
```python
# ขั้นตอน:
1. เปิดโหมดปริ้นอัตโนมัติ
2. เพิ่มสินค้า
3. ชำระเงิน → ควรปริ้นทันที
```

#### Test 3: พรีวิวก่อนปริ้น
```python
# ขั้นตอน:
1. ปิดโหมดปริ้นอัตโนมัติ
2. เพิ่มสินค้า
3. ชำระเงิน → แสดง dialog
4. กด OK → ปริ้น หรือ Cancel → ไม่ปริ้น
```

### 🐛 Troubleshooting

| ปัญหา | วิธีแก้ |
|--------|-------|
| ไม่พบเครื่องปริ้น | ตั้งค่าเครื่องปริ้นใน Dashboard |
| PDF ไม่ปริ้น | ตรวจสอบโหมดปริ้น (auto_print) |
| บาร์โค้ดไม่แสดง | ลองสร้างใบเสร็จใหม่ |
| ข้อความไทยไม่ถูก | ตรวจสอบการโหลด Kanit font |

### 📊 ข้อมูลที่เก็บ

**ในแต่ละใบเสร็จ:**
- เลขที่ใบเสร็จ
- วันที่เวลา
- รายการสินค้า (ชื่อ, จำนวน, ราคา, รวม)
- ยอดรวม, ส่วนลด, ยอดสุดท้าย
- วิธีชำระเงิน
- โค้ตที่ใช้และได้รับ
- บาร์โค้ด

### 🚀 Changelog

**v1.0** (January 9, 2026)
- ✅ สร้างใบเสร็จ PDF
- ✅ สร้างบาร์โค้ด Code128
- ✅ ปริ้นผ่านเครื่องปริ้น 80mm
- ✅ โหมดปริ้นอัตโนมัติ
- ✅ บันทึกไฟล์ PDF
- ✅ UI components
- ✅ Configuration JSON

### 🎓 เอกสารประกอบ

1. **[RECEIPT_PDF_GUIDE.md](RECEIPT_PDF_GUIDE.md)** - คู่มือผู้ใช้ละเอียด
2. **[RECEIPT_TECHNICAL_DOCS.md](RECEIPT_TECHNICAL_DOCS.md)** - เอกสารสำหรับนักพัฒนา
3. **[README.md](README.md)** - เอกสารหลักโปรแกรม

### 💡 Tips & Tricks

- 💾 **บันทึก PDF ให้ปลอดภัย**: PDF จะบันทึกลง `receipts/` ทุกครั้ง
- 🖨️ **ปริ้นหลายครั้ง**: สามารถปริ้นใบเสร็จเดิมได้จากโฟลเดอร์
- 🔧 **ปรับแต่ง**: แก้ไขรูปแบบใบเสร็จในฟังก์ชัน `generate_receipt_pdf()`
- 🌐 **อัปโหลด PDF**: อาจเพิ่มคุณสมบัติ Google Drive ในอนาคต

### ❓ FAQ

**Q: ใบเสร็จจะบันทึกไว้นานเท่าไหร่?**  
A: นานเท่าที่คุณเก็บไฟล์ไว้ ไม่มีการลบอัตโนมัติ

**Q: สามารถเปลี่ยนรูปแบบใบเสร็จได้หรือ?**  
A: ได้ แก้ไขฟังก์ชัน `generate_receipt_pdf()` ใน app_stock.py

**Q: ปริ้นไม่ได้ต้องทำอย่างไร?**  
A: ลองตั้งค่าเครื่องปริ้นใหม่หรือเปิด PDF ด้วยตัวเลือก "Print"

**Q: PDF เก็บที่ไหน?**  
A: โฟลเดอร์ `receipts/` ในที่เดียวกับ app_stock.py

### 📞 ติดต่อและสนับสนุน

- 📧 Email: bringtojz@gmail.com
- 📝 Issues: บันทึกปัญหาเมื่อพบ
- 💬 Suggestions: ส่งแนวคิดใหม่

---

**ขอบคุณที่ใช้ระบบ Stock POS!** 🎉  
**Happy Receipt Printing!** 🖨️✨
